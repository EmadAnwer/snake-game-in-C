#include "snake.h"

/**
 * header - print Game header
 * @points: game points
 * @level: game level
 */
void header(int points, int level)
{
	printf(LINE);
	printf("*  points: %d                   level: %d  *\n", points, level);
/*	printf(LINE);*/
}
